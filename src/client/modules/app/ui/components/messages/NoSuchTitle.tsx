import * as React from "react";
import Message from "./Message";

const NoSuchTitle = () => {
  return (
    <Message message={"As far as we know, this title doesn't exist yet 🙂"} />
  );
};

export default NoSuchTitle;
